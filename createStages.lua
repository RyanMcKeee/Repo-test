--Services
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- Variables
local Stages = ReplicatedStorage:WaitForChild("Stages)
local DefaultStages = Stages:WaitForChild("DefaultStages")
local StageTemplate = script.Parent:WaitForChild("StageTemplate")

for i,stage in pairs(DefaultStages:GetChildren()) do
	
end
